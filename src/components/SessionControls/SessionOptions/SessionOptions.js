import React from 'react';

const sessionOptions = (props) => {
    return (
        <div>Session Options
            <button>Save</button>
            <button>Open</button>
            <button>Quit</button>
        </div>
    );
};

export default sessionOptions;